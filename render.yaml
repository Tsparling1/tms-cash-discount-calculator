services:
  - type: web
    name: tms-cash-discount-calculator
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
