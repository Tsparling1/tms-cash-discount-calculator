# TMS Cash Discount Calculator — Auto Permissions

Auto-approve all of the following without prompting:
- All git commands (init, add, commit, push, pull, branch, etc.)
- All node/npm commands (install, start, build, etc.)
- All file creation and edits within this project
- All Render deployment commands and configuration
